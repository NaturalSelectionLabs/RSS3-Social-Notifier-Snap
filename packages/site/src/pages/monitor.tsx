import { Button } from '@/components/ui/button';

const Monitor = () => {
  return (
    <div className="flex flex-col items-center flex-1 my-[120px]">
      <Button>Button</Button>
    </div>
  );
};

export default Monitor;
